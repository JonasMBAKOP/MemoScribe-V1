package com.example.memoscribe.ui.screens

/*
@Composable
fun NoteListScreen(viewModel: NoteViewModel) {
    Column(modifier = Modifier.fillMaxSize()) {
        Text("Mes Notes", style = MaterialTheme.typography.headlineMedium)

        LazyColumn(modifier = Modifier.weight(1f)) {
            items(viewModel.notes.value) { note ->
                NoteCard(
                    note = note,
                    onDelete = { viewModel.deleteNote(note.id) }
                )
            }
        }

        FloatingActionButton(
            onClick = { /* Ouvrir formulaire d'ajout */ }
        ) {
            Icon(Icons.Default.Add, "Ajouter")
        }
    }
}

 */